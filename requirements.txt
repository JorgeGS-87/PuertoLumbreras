# Framework web
Flask==3.0.0
Flask-CORS==4.0.0
Flask-SQLAlchemy==3.0.3

# Librerías básicas (versiones recomendadas)
numpy==1.25.2
pandas==2.2.3
matplotlib==3.8.1

# GIS - Datos espaciales
# NOTE: On Windows it's recommended to install geopandas and its binary deps via conda
# (conda install -c conda-forge geopandas) or use prebuilt wheels.
geopandas==0.13.2
shapely==2.1.3
pyproj==3.7.0

# Cálculo de rutas
networkx==3.5

# Base de datos — MongoDB
pymongo==4.6.1
bcrypt==4.1.2

# Tiempo real — WebSockets
Flask-SocketIO==5.3.6
eventlet==0.35.2